apply plugin: "war"

bootWar {
    mainClassName = "com.ft.ViettelMpsConnectorApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]
}
